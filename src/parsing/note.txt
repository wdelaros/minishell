"" = interprete les variables
'' = string literal, char par char tel quel

a faire:
	gérer le parsing du echo
	gérer les $
		.Prendre la len de la variable
		.Realloc mon input
		.Aller remplacer les char de la var par la variable
	gérer le $? (voir le PDF)
	gérer les signaux

a régler:

echo '$USER'"$USER"'$USER'